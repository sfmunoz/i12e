apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anki
  namespace: anki
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 128Mi
