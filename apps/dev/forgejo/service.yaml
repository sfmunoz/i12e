apiVersion: v1
kind: Service
metadata:
  name: ${APPNAME}
  namespace: ${NAMESPACE}
spec:
  selector:
    app: ${APPNAME}
  ports:
  - name: ssh
    port: 2222
    targetPort: 22
  - name: http
    port: 3000
    targetPort: 3000
