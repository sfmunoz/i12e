apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trilium
  namespace: trilium
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 128Mi
