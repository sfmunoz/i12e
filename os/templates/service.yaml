apiVersion: v1
kind: Service
metadata:
  name: {{ include "os.fullname" . }}
  labels:
    {{- include "os.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "os.selectorLabels" . | nindent 4 }}
