apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: certs
  namespace: cert-manager
spec:
  interval: 17m12s
  chart:
    spec:
      chart: ./misc/certs
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
