apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-rclone
  namespace: veloxpack
spec:
  chartRef:
    kind: OCIRepository
    name: csi-rclone
    namespace: flux-system
  interval: 10m53s
