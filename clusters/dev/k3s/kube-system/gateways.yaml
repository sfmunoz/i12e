apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: http-8000
  namespace: kube-system
spec:
  gatewayClassName: traefik
  listeners:
  - allowedRoutes:
      namespaces:
        from: All
    name: web
    port: 8000
    protocol: HTTP
